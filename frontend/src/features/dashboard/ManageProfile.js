import React from 'react';
import { Typography } from '@mui/material';

const ManageProfile = () => (
  <Typography variant="h5">Manage Profile (Coming Soon)</Typography>
);

export default ManageProfile; 